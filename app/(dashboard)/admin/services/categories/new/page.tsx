
import ServiceForm from '../../ServiceForm';

export default function CreateCategoryPage() {
    return (
        <ServiceForm
            type="category"
            categories={[]}
        />
    );
}
