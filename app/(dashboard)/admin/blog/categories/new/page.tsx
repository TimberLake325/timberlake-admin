"use client";

import React from 'react';
import BlogForm from '../../pageData';

export default function CreateCategoryPage() {
    return (
        <BlogForm
            type="category"
            categories={[]} 
        />
    );
}
